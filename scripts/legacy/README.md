# Additional benchmark variants

This directory contains supplementary benchmark and validation variants for comparing different model sizes and runtime settings.

Run these scripts from the repository root so that dataset paths and output locations resolve correctly.
